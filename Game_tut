'''A bit Racey'''

initiate pygame : pygame.init()

set a display surface :

		pygame.display.set_mode(width,height)

		pygame.display.set_caption("name") method sets icon

game clock:
	pygame.time.Clock()
	time.sleep(2) #sleeps for 2 sec
	
	clock.tick(60) #sets frame persecond



loading image:
	x = pygame.image.load("url")

loading music:
	sound_  = pygame.mixer.Sound("url")

	music_ = pygame.mixer.load("url")

playing music:
	pygame.mixer.music.play(-1)
		 #-1 play indefinetly

	pygame.mixer.music.unpause()
	pygame.mixer.music.pause()
	pygame.mixer.music.stop()

	pygame.mixer.Sound.play(car_crash)




How to Display text:
	font = pygame.font.SysFont(None,25)
	text  = font.render("Doged: "+str(count),True,black)
	
	gameDisplay.blit(text,(0,0))


how to update:
	pygame.display.update()


quiting the game:
	#uninitiate pygame
	pygame.quit()#stops
	quit()

event_handeling  :
	for event in pygame.event.get():
		if event.type == pygame.QUIT:

		if event.type == pygame.KEYDOWN:

		if event.key == pygame.K_LEFT

		pygame.K_thkey

		if event.type == pygame.KEYUP:

	mouse:
		mouse = pygame.mouse.get_pos()
		#it gives tuple of location of mouse x,y
		
		click = pygame.mouse.get_pressed()
		#three values left,mid ryt 0 0 0





